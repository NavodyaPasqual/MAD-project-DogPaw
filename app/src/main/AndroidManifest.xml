<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dogpaw">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ExtendPhotography"></activity>
        <activity android:name=".PhotographyBooking" />
        <activity android:name=".AdminPhotographyAdd" />
        <activity android:name=".AdminPhotographyUpdate" />
        <activity android:name=".AdminPhotographyManagemnt" />
        <activity android:name=".Payment" />
        <activity android:name=".Photography_second" />
        <activity android:name=".Photography_first" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>